
<div class="container">
	<div class="row">
		<div class="col s6 center-align">
			<h2>What do you want to eat?</h2>
			{{#each name}}
			<ul class="burgerItem">
				<li><h4>{{this.burger_name}}</h4> <button class="btn">Devour</button></li>
			</ul>
			{{/each}}
		</div>
		<div class="col s6 center-align" id="inBase">
			<h2>Devoured</h2>
			{{#each name}}
			<ul class="burgerItem">
				<li><h4>{{this.burger_name}}</h4> <button class="btn">Devour</button></li>
			</ul>
			{{/each}}
		</div>
	</div>
	<div class="row center-align">
		<input type="text" name="nameOfNewBurger" id="nameOfNewBurger">
		<button class="btn" id="bAdd">Add!</button>
	</div>
</div>
